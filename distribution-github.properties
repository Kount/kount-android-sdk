# Kount Data Collector - Distribution Metadata (GitHub)
# This file identifies the source distribution of this jar

# Distribution source identifier
distribution.source=github

# Version information
version.name=5.0.3

# GitHub-specific metadata
distribution.repository=https://github.com/Kount/kount-android-sdk
distribution.release.type=github-release

# Build metadata (populated at build time)
build.timestamp=${BUILD_TIMESTAMP}
build.gradle.version=${GRADLE_VERSION}
