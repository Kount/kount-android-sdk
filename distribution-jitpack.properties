# Kount Data Collector - Distribution Metadata (JitPack)
# This file identifies the source distribution of this jar

# Distribution source identifier
distribution.source=jitpack

# Version information
version.name=5.0.3

# JitPack-specific metadata
distribution.repository=https://jitpack.io
distribution.release.type=jitpack-build

# Build metadata (populated at build time)
build.timestamp=${BUILD_TIMESTAMP}
build.gradle.version=${GRADLE_VERSION}
